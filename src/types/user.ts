export type TUserResponse = {
  success: boolean;
  user: {
    email: string;
    name: string;
  };
};

export type TUserUpdateResponse = {
  success: boolean;
  user: {
    email: string;
    name: string;
  };
};
